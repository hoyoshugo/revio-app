providers = ["node"]

[phases.install]
cmds = [
  "cd frontend && npm ci",
  "cd backend && npm ci --omit=dev"
]

[phases.build]
cmds = ["cd frontend && npm run build"]

[start]
cmd = "node backend/src/index.js"
